# 🚀 ЗАПУСК И ТЕСТИРОВАНИЕ - ГОТОВО!

## ✅ Всё настроено для Android Studio эмулятора!

API_BASE обновлён во всех файлах на `http://10.0.2.2:3000` (для Android эмулятора).

## Быстрый запуск (3 шага)

### Шаг 1: Установите Expo Go на эмуляторе

1. На эмуляторе откройте **Google Play Store**
2. Найдите **"Expo Go"**
3. Установите и откройте

### Шаг 2: Запустите Backend (если еще не запущен)

```powershell
cd drops-crypto-api
npm run start:dev
```

Проверка:
```powershell
Invoke-RestMethod -Uri http://localhost:3000/health
```
Должно вернуть: `{"ok":true}`

### Шаг 3: Запустите Mobile App

```powershell
cd drops-crypto-app
npm start
```

В терминале Expo:
- **Нажмите `a`** для запуска на Android эмуляторе
- ИЛИ откройте Expo Go на эмуляторе и отсканируйте QR код

## Тестирование

### ✅ Проверка загрузки
- Приложение должно открыться в Expo Go
- Должен появиться экран авторизации

### ✅ Тест OAuth (требует ngrok)
Для полноценного теста OAuth используйте ngrok:

1. Запустите ngrok:
   ```powershell
   ngrok http 3000
   ```

2. Обновите API_BASE на ngrok URL:
   ```typescript
   const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
   ```

3. Настройте Twitch Developer Console с ngrok URL

4. Протестируйте OAuth флоу

### ✅ Тест без OAuth (локально)
Можно протестировать UI и функции без OAuth:

1. **Экраны:**
   - Переключайтесь между вкладками
   - Проверьте UI

2. **API endpoints:**
   ```powershell
   # Список стримеров
   Invoke-RestMethod -Uri http://localhost:3000/streamers
   
   # Список призов
   Invoke-RestMethod -Uri http://localhost:3000/prizes
   ```

## Важные заметки

### Для Android эмулятора:
- ✅ Используйте: `http://10.0.2.2:3000`
- ✅ `10.0.2.2` - специальный IP для доступа к localhost компьютера

### Для ngrok (OAuth):
- ✅ Используйте: `https://ваш-ngrok-url.ngrok-free.app`
- ✅ Работает как на эмуляторе, так и на реальном устройстве

### Для реального устройства:
- ✅ Используйте ngrok URL
- ✅ Или IP вашей локальной сети (если в той же сети)

## Что тестировать

- [x] Приложение загружается на эмуляторе
- [ ] Экран авторизации отображается
- [ ] Экраны переключаются (Стримеры, Призы, Профиль)
- [ ] API работает (если backend запущен)
- [ ] UI выглядит корректно

## Документация

- `ANDROID_STUDIO_EMULATOR.md` - полная инструкция
- `TESTING_GUIDE.md` - детальное руководство по тестированию

---

**Всё готово! Запускайте и тестируйте!** 🎉
