# 📱 Запуск на Android Studio эмуляторе

## ✅ Да, это работает отлично!

Android Studio эмулятор - идеальный вариант для тестирования React Native + Expo приложений.

## Быстрая инструкция

### Шаг 1: Убедитесь, что эмулятор запущен

**Способ A: Через Android Studio (рекомендуется)**
1. Откройте Android Studio
2. `Tools → Device Manager`
3. Найдите ваш эмулятор
4. Нажмите ▶ (Play) для запуска
5. Дождитесь полной загрузки

**Способ B: Через командную строку**
```powershell
# Найти путь к эмулятору
$emulatorPath = "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe"

# Список доступных эмуляторов
& $emulatorPath -list-avds

# Запустить конкретный эмулятор
& $emulatorPath -avd имя_эмулятора
```

### Шаг 2: Проверьте подключение

После запуска эмулятора проверьте:

```powershell
# Если adb в PATH:
adb devices

# Должно показать:
# emulator-5554   device
```

Если `adb` не найден, добавьте в PATH:
```powershell
# Путь к platform-tools обычно:
$env:Path += ";$env:LOCALAPPDATA\Android\Sdk\platform-tools"
```

### Шаг 3: Установите Expo Go на эмуляторе

1. На эмуляторе откройте **Google Play Store**
2. Найдите **"Expo Go"**
3. Установите приложение
4. Откройте Expo Go

### Шаг 4: Настройте API_BASE для эмулятора

**Важно:** Android эмулятор использует специальный IP для доступа к localhost вашего компьютера.

Обновите во всех файлах:
- `drops-crypto-app/App.tsx`
- `drops-crypto-app/screens/*.tsx`

```typescript
// Для Android Studio эмулятора используйте:
const API_BASE = 'http://10.0.2.2:3000';

// ИЛИ если используете ngrok (рекомендуется для OAuth):
const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
```

**Почему `10.0.2.2`?**
- `localhost` или `127.0.0.1` на эмуляторе указывает на сам эмулятор, а не на ваш компьютер
- `10.0.2.2` - это специальный IP, который Android эмулятор использует для доступа к `localhost` хост-машины

### Шаг 5: Запустите Backend

```powershell
cd drops-crypto-api
npm run start:dev
```

Проверьте, что работает:
```powershell
Invoke-RestMethod -Uri http://localhost:3000/health
```

### Шаг 6: Запустите Mobile App

```powershell
cd drops-crypto-app
npm start
```

В терминале Expo появится меню:
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web
```

**Нажмите `a`** для запуска на Android эмуляторе.

Или:
- Откройте Expo Go на эмуляторе
- Нажмите "Scan QR code"
- Отсканируйте QR код из терминала

### Шаг 7: Тестирование

1. **Проверка загрузки:**
   - Приложение должно открыться в Expo Go на эмуляторе
   - Должен появиться экран авторизации

2. **Тест OAuth:**
   - Нажмите "Привязать Twitch"
   - Откроется браузер на эмуляторе
   - Войдите в Twitch
   - Должно вернуться в приложение (deep linking)

3. **Тест экранов:**
   - Переключайтесь между вкладками (Стримеры, Призы, Профиль)
   - Проверьте загрузку данных

4. **Тест функций:**
   - Добавьте кошелёк в профиле
   - Попробуйте получить приз

## Преимущества Android Studio эмулятора

✅ Быстрый и стабильный  
✅ Полная эмуляция Android  
✅ Интеграция с Android Studio  
✅ Можно тестировать разные версии Android  
✅ Поддержка hardware acceleration  

## Решение проблем

### Эмулятор не запускается

**Решение:**
1. Убедитесь, что HAXM установлен (для Intel)
2. Проверьте, что виртуализация включена в BIOS
3. Закройте другие эмуляторы/виртуальные машины
4. Увеличьте RAM в настройках эмулятора

### Expo Go не видит эмулятор

**Решение:**
```powershell
# Перезапустить ADB
adb kill-server
adb start-server
adb devices
```

### Backend недоступен с эмулятора

**Проблема:** `http://10.0.2.2:3000` не работает

**Решения:**
1. Убедитесь, что backend запущен: `Invoke-RestMethod -Uri http://localhost:3000/health`
2. Проверьте firewall - он может блокировать подключения
3. Используйте ngrok (лучше для OAuth):
   ```powershell
   ngrok http 3000
   # Используйте ngrok URL вместо 10.0.2.2:3000
   ```

### Deep linking не работает на эмуляторе

**Проблема:** После OAuth не возвращается в приложение

**Решение:**
- Deep linking может работать не так стабильно на эмуляторе
- Для полного тестирования OAuth используйте реальное устройство
- Или тестируйте OAuth через браузер эмулятора вручную

### Медленная работа эмулятора

**Решения:**
1. Увеличьте RAM эмулятора (минимум 2GB, лучше 4GB)
2. Включите Hardware Acceleration (HAXM)
3. Используйте x86/x86_64 образ (не ARM)
4. Закройте ненужные приложения на компьютере

## Альтернативные способы

### Способ 1: Использовать ngrok (рекомендуется для OAuth)

```powershell
# Запустить ngrok
ngrok http 3000

# Использовать ngrok URL в API_BASE
const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
```

Преимущества:
- Работает как на эмуляторе, так и на реальном устройстве
- OAuth работает корректно
- Можно тестировать с разных устройств одновременно

### Способ 2: Web версия (для быстрого тестирования UI)

```powershell
cd drops-crypto-app
npm start
# Нажмите 'w'
```

Откроется в браузере. Ограничения: deep linking может не работать.

## Чеклист запуска

- [ ] Android Studio установлен
- [ ] Эмулятор создан и запущен
- [ ] Expo Go установлен на эмуляторе
- [ ] API_BASE настроен на `http://10.0.2.2:3000` или ngrok URL
- [ ] Backend запущен на `localhost:3000`
- [ ] Mobile App запущен через `npm start`
- [ ] Приложение открылось в Expo Go на эмуляторе

---

**Готово! Теперь можно тестировать на Android Studio эмуляторе!** 🚀
