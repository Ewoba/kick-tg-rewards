# 🧪 Руководство по тестированию и запуску

## Шаг 1: Запуск инфраструктуры

### 1.1 Проверка Docker

Убедитесь, что Docker Desktop запущен. Проверьте:
```powershell
docker ps
```

Должны быть видны контейнеры:
- `drops-crypto-api-postgres-1`
- `drops-crypto-api-redis-1`

Если их нет, запустите:
```powershell
cd drops-crypto-api
docker compose up -d
```

### 1.2 Проверка базы данных

```powershell
cd drops-crypto-api
npx prisma migrate status
```

Если есть неприменённые миграции:
```powershell
npx prisma migrate dev
```

## Шаг 2: Заполнение тестовыми данными (опционально)

```powershell
cd drops-crypto-api
npm run prisma:seed
```

Это создаст:
- 2 тестовых стримера
- 2 тестовых приза

## Шаг 3: Запуск Backend

### 3.1 Настройка Twitch ключей

1. Откройте `drops-crypto-api\.env`
2. Замените:
   ```
   TWITCH_CLIENT_ID=your_twitch_client_id_here
   TWITCH_CLIENT_SECRET=your_twitch_client_secret_here
   ```
   на ваши реальные ключи с https://dev.twitch.tv/console/apps

### 3.2 Запуск Backend

```powershell
cd drops-crypto-api
npm run start:dev
```

Backend должен запуститься на `http://localhost:3000`

Проверка:
```powershell
# В другом терминале
Invoke-RestMethod -Uri http://localhost:3000/health
```

Должно вернуть: `{"ok":true}`

## Шаг 4: Настройка ngrok

### 4.1 Установка ngrok (если не установлен)

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-ngrok.ps1
```

Или скачайте с https://ngrok.com/download

### 4.2 Запуск ngrok

В **новом терминале**:
```powershell
ngrok http 3000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

### 4.3 Обновление URL везде

```powershell
.\update-ngrok-url.ps1 -NgrokUrl "https://ваш-ngrok-url.ngrok-free.app"
```

Или обновите вручную:

**В `drops-crypto-api\.env`:**
```env
PUBLIC_BASE_URL=https://ваш-ngrok-url.ngrok-free.app
TWITCH_REDIRECT_URI=https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback
```

**В `drops-crypto-app\App.tsx` и всех screen файлах:**
```typescript
const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
```

### 4.4 Настройка Twitch Developer Console

1. Перейдите на https://dev.twitch.tv/console/apps
2. Откройте ваше приложение
3. В разделе **"OAuth Redirect URLs"** добавьте:
   ```
   https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback
   ```
4. Сохраните изменения

### 4.5 Перезапуск Backend

После изменения `.env` перезапустите backend:
1. Остановите (Ctrl+C)
2. Запустите снова: `npm run start:dev`

## Шаг 5: Запуск Mobile App

### 5.1 Обновление API_BASE

Убедитесь, что во всех файлах экранов указан правильный ngrok URL:
- `drops-crypto-app/App.tsx`
- `drops-crypto-app/screens/AuthScreen.tsx`
- `drops-crypto-app/screens/ProfileScreen.tsx`
- `drops-crypto-app/screens/StreamersScreen.tsx`
- `drops-crypto-app/screens/PrizesScreen.tsx`

### 5.2 Запуск

```powershell
cd drops-crypto-app
npm start
```

Откройте Expo Go на телефоне и отсканируйте QR код.

## Шаг 6: Тестирование функциональности

### 6.1 Тест Health Endpoint

**Backend (локально):**
```powershell
Invoke-RestMethod -Uri http://localhost:3000/health
```

**Через ngrok (с телефона или компьютера):**
```
https://ваш-ngrok-url.ngrok-free.app/health
```

Ожидаемый результат: `{"ok":true}`

### 6.2 Тест OAuth флоу

1. Откройте приложение на телефоне
2. Должен появиться экран авторизации
3. Нажмите "Привязать Twitch"
4. Должен открыться браузер с Twitch login
5. Войдите в Twitch
6. Должно вернуться в приложение
7. Должен открыться главный экран с вкладками

### 6.3 Тест экранов приложения

**Экран Streamers:**
- Должен показываться список стримеров
- Если нет данных - показывается "Нет активных стримеров"

**Экран Prizes:**
- Должен показываться список призов
- Если нет данных - показывается "Нет доступных призов"

**Экран Profile:**
- Должен показываться Twitch логин
- Статус участия
- Форма для добавления кошелька

### 6.4 Тест добавления кошелька

1. Перейдите на экран Profile
2. Введите адрес кошелька (формат: `0x...` 40 символов)
3. Нажмите "Добавить"
4. Должно появиться сообщение об успехе
5. Кошелёк должен отобразиться в профиле
6. Статус должен стать "✅ Активно"

### 6.5 Тест получения приза

1. Перейдите на экран Prizes
2. Найдите доступный приз
3. Нажмите "Получить приз"
4. Должно появиться сообщение об успехе
5. Приз должен быть отмечен как полученный

## Шаг 7: Тестирование API вручную

### 7.1 Список стримеров

```powershell
Invoke-RestMethod -Uri https://ваш-ngrok-url/streamers
```

### 7.2 Список призов

```powershell
Invoke-RestMethod -Uri https://ваш-ngrok-url/prizes
```

### 7.3 Получение данных пользователя (требует токен)

Сначала получите токен через OAuth, затем:

```powershell
$token = "ваш_jwt_токен"
$headers = @{
    "Authorization" = "Bearer $token"
}
Invoke-RestMethod -Uri https://ваш-ngrok-url/me -Headers $headers
```

## 🐛 Решение проблем

### Backend не запускается

**Проблема:** Ошибки при запуске

**Решение:**
1. Проверьте, что PostgreSQL запущен: `docker ps`
2. Проверьте `.env` файл
3. Проверьте логи: `docker compose logs postgres`

### ngrok URL не работает

**Проблема:** Не могу открыть через ngrok

**Решение:**
1. Убедитесь, что backend запущен на localhost:3000
2. Проверьте, что ngrok запущен
3. Убедитесь, что URL правильный (HTTPS, не HTTP)

### OAuth не возвращает в приложение

**Проблема:** После авторизации не возвращается в приложение

**Решение:**
1. Проверьте scheme в `app.json`: должно быть `"scheme": "dropscrypto"`
2. Проверьте `APP_DEEPLINK_SCHEME` в `.env`: должно быть `dropscrypto`
3. Проверьте Redirect URL в Twitch Console
4. Переустановите приложение в Expo Go

### Призы/Стримеры не загружаются

**Проблема:** Пустые списки

**Решение:**
1. Заполните тестовыми данными: `npm run prisma:seed`
2. Проверьте, что API работает: `Invoke-RestMethod -Uri https://ваш-url/streamers`
3. Проверьте логи backend на ошибки

### Ошибка при получении приза

**Проблема:** "Wallet required to claim prize"

**Решение:**
1. Убедитесь, что кошелёк добавлен в профиле
2. Проверьте формат адреса (0x... 40 символов)
3. Проверьте, что токен валидный

## ✅ Чеклист тестирования

- [ ] Docker контейнеры запущены
- [ ] Backend запущен и отвечает на /health
- [ ] ngrok запущен и доступен
- [ ] Twitch ключи настроены
- [ ] Twitch Developer Console настроен
- [ ] Mobile App запущен
- [ ] OAuth работает (возвращает в приложение)
- [ ] Экраны загружаются
- [ ] Можно добавить кошелёк
- [ ] Можно получить приз
- [ ] Профиль отображается корректно

## 📝 Скрипт быстрого тестирования

Создайте файл `test-all.ps1`:

```powershell
Write-Host "=== Тестирование проекта ===" -ForegroundColor Cyan

Write-Host "`n1. Проверка Docker..." -ForegroundColor Yellow
docker ps | Select-String "postgres|redis"

Write-Host "`n2. Проверка Backend..." -ForegroundColor Yellow
try {
    $health = Invoke-RestMethod -Uri http://localhost:3000/health
    Write-Host "✅ Backend работает: $($health | ConvertTo-Json)" -ForegroundColor Green
} catch {
    Write-Host "❌ Backend не отвечает" -ForegroundColor Red
}

Write-Host "`n3. Проверка ngrok..." -ForegroundColor Yellow
try {
    $ngrok = Invoke-RestMethod -Uri http://localhost:4040/api/tunnels
    if ($ngrok.tunnels) {
        Write-Host "✅ ngrok работает: $($ngrok.tunnels[0].public_url)" -ForegroundColor Green
    } else {
        Write-Host "⚠ ngrok запущен, но туннели не найдены" -ForegroundColor Yellow
    }
} catch {
    Write-Host "❌ ngrok не запущен" -ForegroundColor Red
}

Write-Host "`n=== Готово ===" -ForegroundColor Cyan
```

Запустите:
```powershell
.\test-all.ps1
```

---

**Готово! Теперь можно тестировать приложение!** 🚀
