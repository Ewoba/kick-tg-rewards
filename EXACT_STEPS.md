# ⚡ ТОЧНЫЕ ШАГИ - ВЫПОЛНИТЬ СЕЙЧАС

## 📝 Шаг 1: .env файл

**Откройте:** `C:\Users\Admin\Downloads\drops\drops-crypto-api\.env`

**Вставьте в таком виде (каждая переменная с новой строки):**

```
TWITCH_CLIENT_ID=ваш_client_id
TWITCH_CLIENT_SECRET=ваш_client_secret
TWITCH_REDIRECT_URI=http://localhost:3000/auth/twitch/callback
PUBLIC_BASE_URL=http://localhost:3000
APP_DEEPLINK_SCHEME=dropscrypto
JWT_SECRET=любая_длинная_случайная_строка
```

**⚠️ ВАЖНО:**
- Каждая переменная должна быть на **ОТДЕЛЬНОЙ СТРОКЕ**
- После каждой строки нажмите **Enter** (переход на новую строку)
- Не должно быть все в одной строке!

---

## 🔄 Шаг 2: Перезапустить backend

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-api
npm run start:dev
```

**Дождитесь:** `Nest application successfully started`

**Проверка:**
```
http://localhost:3000/health → { "ok": true }
```

---

## 🔐 Шаг 3: Пройти OAuth flow

**Откройте в браузере:**
```
http://localhost:3000/auth/twitch/start
```

**Дальше:**
1. Twitch покажет экран авторизации
2. Нажмите **"Authorize"**
3. Twitch перенаправит на `/auth/twitch/callback`

**⚠️ Нюанс:** После callback может быть редирект на `dropscrypto://...` — на ПК это нормально, если не откроется.

---

## 🔍 Шаг 4: Проверить в Prisma Studio

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-api
npx prisma studio
```

**В Prisma Studio:**
1. Откройте таблицу **User**
2. Найдите запись с `twitchLogin = tiktak6828`

---

## ❓ Ответить

**После шагов 1-4 напишите одним словом:**

- **"Да"** — если запись появилась в таблице User
- **"Нет"** — если записи нет или ошибка

**Если "Нет"** — пришлите текст ошибки со страницы Twitch (чаще всего `redirect_uri_mismatch`).

---

**Выполняйте шаги по порядку. После шага 4 напишите: "Да" или "Нет"** ✅
