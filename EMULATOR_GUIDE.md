# 📱 Тестирование на эмуляторе

## Варианты эмуляторов

### Вариант 1: Android Studio Emulator (рекомендуется для Windows)

#### Установка Android Studio

1. Скачайте Android Studio: https://developer.android.com/studio
2. Установите Android Studio
3. Откройте Android Studio
4. Перейдите: `Tools → SDK Manager`
5. Вкладка `SDK Tools`, установите:
   - ✅ Android SDK Platform-Tools
   - ✅ Android SDK Build-Tools
   - ✅ Android Emulator
   - ✅ Intel x86 Emulator Accelerator (HAXM installer)

#### Создание эмулятора

1. В Android Studio: `Tools → Device Manager`
2. Нажмите `Create Device`
3. Выберите устройство (например, Pixel 5)
4. Выберите системный образ (рекомендуется API 33 или 34)
5. Завершите создание

#### Запуск эмулятора

**Через Android Studio:**
1. `Tools → Device Manager`
2. Нажмите ▶ рядом с нужным эмулятором

**Через командную строку:**
```powershell
# Найти путь к эмулятору
$emulatorPath = "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe"

# Список доступных эмуляторов
& $emulatorPath -list-avds

# Запустить эмулятор
& $emulatorPath -avd имя_эмулятора
```

### Вариант 2: Expo Go на эмуляторе

#### Android Emulator

1. Запустите Android эмулятор
2. Откройте Google Play Store на эмуляторе
3. Найдите и установите **Expo Go**
4. Запустите Expo Go
5. В терминале: `cd drops-crypto-app && npm start`
6. Выберите `a` для Android или отсканируйте QR код

#### iOS Simulator (только на Mac)

1. Установите Xcode из App Store
2. Откройте Xcode → Preferences → Components
3. Установите iOS Simulator
4. Запустите: `open -a Simulator`
5. В терминале: `cd drops-crypto-app && npm start`
6. Выберите `i` для iOS

### Вариант 3: Web версия (быстрый вариант)

Expo может запустить приложение в браузере:

```powershell
cd drops-crypto-app
npm start
# Затем нажмите 'w' для web
```

**Ограничения:** некоторые функции (как deep linking) могут не работать в браузере.

## Пошаговая инструкция для Android Emulator

### Шаг 1: Запуск эмулятора

```powershell
# Способ 1: Через Android Studio
# Откройте Android Studio → Tools → Device Manager → Запустите эмулятор

# Способ 2: Через командную строку
$emulatorPath = "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe"
& $emulatorPath -avd Pixel_5_API_33
```

### Шаг 2: Проверка подключения

```powershell
# Проверить подключенные устройства
adb devices

# Должно показать что-то вроде:
# emulator-5554   device
```

### Шаг 3: Установка Expo Go

**Вариант A: Через Play Store**
1. Откройте Google Play Store на эмуляторе
2. Найдите "Expo Go"
3. Установите

**Вариант B: Через ADB (если Play Store недоступен)**
```powershell
# Скачайте APK с https://expo.dev/tools
# Установите:
adb install path\to\ExpoGo.apk
```

### Шаг 4: Настройка ngrok для локальной сети

Эмулятор должен иметь доступ к ngrok URL. Если используются тестовые данные, можно временно использовать локальный IP:

```powershell
# Узнать локальный IP
ipconfig | Select-String "IPv4"

# Обновить API_BASE в App.tsx на:
const API_BASE = 'http://10.0.2.2:3000';  # Для Android эмулятора
# или
const API_BASE = 'http://ваш_локальный_IP:3000';  # Для реального устройства в той же сети
```

**Но лучше использовать ngrok URL**, так как он доступен и с эмулятора, и с реального устройства.

### Шаг 5: Запуск приложения

```powershell
cd drops-crypto-app
npm start
```

В Expo CLI выберите:
- `a` - для Android эмулятора
- Или отсканируйте QR код в Expo Go на эмуляторе

### Шаг 6: Тестирование

1. Откройте приложение в Expo Go на эмуляторе
2. Протестируйте все функции:
   - Авторизацию через Twitch
   - Просмотр стримеров
   - Просмотр призов
   - Добавление кошелька
   - Получение приза

## Решение проблем

### Эмулятор медленно работает

**Решение:**
1. Увеличьте RAM в настройках эмулятора (минимум 2GB)
2. Включите Hardware Acceleration (HAXM)
3. Используйте x86/x86_64 образ (быстрее чем ARM)

### ADB не видит эмулятор

**Решение:**
```powershell
# Перезапустить ADB сервер
adb kill-server
adb start-server
adb devices
```

### Expo Go не запускается

**Решение:**
1. Убедитесь, что эмулятор полностью загружен
2. Проверьте, что эмулятор подключен: `adb devices`
3. Перезапустите Expo: `npm start`

### Deep linking не работает на эмуляторе

**Решение:**
Deep linking может работать не так, как на реальном устройстве. Для тестирования:
1. Используйте реальное устройство через Expo Go
2. Или тестируйте OAuth через браузер на эмуляторе

### Не могу открыть ngrok URL на эмуляторе

**Решение:**
1. Убедитесь, что эмулятор имеет доступ в интернет
2. Проверьте, что ngrok запущен
3. Откройте ngrok URL в браузере эмулятора для проверки

## Быстрый старт (суммировано)

```powershell
# 1. Запустить эмулятор
# Через Android Studio или командную строку

# 2. Проверить подключение
adb devices

# 3. Установить Expo Go (через Play Store на эмуляторе)

# 4. Запустить backend
cd drops-crypto-api
npm run start:dev

# 5. Запустить ngrok (в другом терминале)
ngrok http 3000

# 6. Обновить API_BASE в App.tsx на ngrok URL

# 7. Запустить мобильное приложение
cd drops-crypto-app
npm start
# Нажать 'a' для Android
```

## Альтернатива: Genymotion

Genymotion - альтернативный Android эмулятор (может быть быстрее):

1. Скачайте: https://www.genymotion.com
2. Установите и создайте устройство
3. Используйте так же, как Android Studio эмулятор

---

**Готово! Теперь можно тестировать на эмуляторе!** 🚀
