# 🚀 Следующие шаги - что делать дальше

## ✅ Что уже готово

- ✅ Docker контейнеры запущены (PostgreSQL и Redis)
- ✅ База данных настроена (миграции выполнены)
- ✅ Backend работает на http://localhost:3000
- ✅ Зависимости установлены (backend и mobile app)
- ✅ Скрипты автоматизации созданы

## 📋 Что нужно сделать сейчас

### Шаг 1: Установить ngrok (если еще не установлен)

#### Вариант A: Автоматическая установка
```powershell
powershell -ExecutionPolicy Bypass -File .\setup-ngrok.ps1
```

#### Вариант B: Ручная установка
1. Перейдите на https://ngrok.com/download
2. Скачайте для Windows
3. Распакуйте `ngrok.exe` в папку (например `C:\ngrok`)
4. Добавьте папку в PATH или используйте полный путь

#### Вариант C: Через Chocolatey (если установлен)
```powershell
choco install ngrok
```

### Шаг 2: Получить Twitch ключи

1. Перейдите на https://dev.twitch.tv/console/apps
2. Войдите в аккаунт Twitch
3. Создайте новое приложение (или используйте существующее)
4. Скопируйте **Client ID**
5. Нажмите **"New Secret"** и скопируйте **Client Secret**

### Шаг 3: Настроить Twitch ключи в .env

Откройте файл `drops-crypto-api\.env` и замените:

```env
TWITCH_CLIENT_ID=your_twitch_client_id_here
TWITCH_CLIENT_SECRET=your_twitch_client_secret_here
```

на:

```env
TWITCH_CLIENT_ID=ваш_реальный_client_id
TWITCH_CLIENT_SECRET=ваш_реальный_client_secret
```

### Шаг 4: Запустить ngrok

В новом терминале:

```powershell
ngrok http 3000
```

Вы увидите что-то вроде:
```
Forwarding   https://abc123.ngrok-free.app -> http://localhost:3000
```

**Скопируйте этот HTTPS URL** (например: `https://abc123.ngrok-free.app`)

### Шаг 5: Обновить URL везде

#### Автоматически (рекомендуется):
```powershell
.\update-ngrok-url.ps1 -NgrokUrl "https://ваш-ngrok-url.ngrok-free.app"
```

#### Или вручную:

1. **В `drops-crypto-api\.env`** обновите:
   ```env
   PUBLIC_BASE_URL=https://ваш-ngrok-url.ngrok-free.app
   TWITCH_REDIRECT_URI=https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback
   ```

2. **В `drops-crypto-app\App.tsx`** обновите:
   ```typescript
   const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
   ```

### Шаг 6: Настроить Twitch Developer Console

1. Перейдите на https://dev.twitch.tv/console/apps
2. Откройте ваше приложение
3. В разделе **"OAuth Redirect URLs"** добавьте:
   ```
   https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback
   ```
4. **Сохраните** изменения

### Шаг 7: Перезапустить backend

После изменения `.env` файла нужно перезапустить backend:

1. Остановите текущий процесс backend (Ctrl+C в окне где он запущен)
2. Запустите снова:
   ```powershell
   cd drops-crypto-api
   npm run start:dev
   ```

Или используйте скрипт:
```powershell
cd drops-crypto-api
.\start-backend.ps1
```

### Шаг 8: Запустить мобильное приложение

```powershell
cd drops-crypto-app
npm start
```

Откройте Expo Go на телефоне и отсканируйте QR код.

## 🧪 Проверка работоспособности

После выполнения всех шагов:

1. **Проверьте backend health:**
   - Откройте в браузере: http://localhost:3000/health
   - Должно вернуть: `{"ok":true}`

2. **Проверьте ngrok health:**
   - Откройте на телефоне: https://ваш-ngrok-url.ngrok-free.app/health
   - Должно вернуть: `{"ok":true}`

3. **Проверьте OAuth:**
   - В мобильном приложении нажмите **"Привязать Twitch"**
   - Должен открыться Twitch login
   - После авторизации должно вернуться в приложение
   - В приложении должно показаться **"Token: есть"**

## 🔍 Проверка статуса

Используйте скрипт проверки:

```powershell
powershell -ExecutionPolicy Bypass -File .\check-status.ps1
```

## ⚠️ Частые проблемы

### ngrok URL меняется при перезапуске

Это нормально. Просто обновите URL везде снова:
```powershell
.\update-ngrok-url.ps1 -NgrokUrl "новый-url"
```

### Backend не отвечает

1. Проверьте, что он запущен: `Get-Process node`
2. Проверьте логи на ошибки
3. Перезапустите: `cd drops-crypto-api; npm run start:dev`

### OAuth не работает

1. Проверьте, что Twitch ключи правильные в `.env`
2. Проверьте, что Redirect URL совпадает в Twitch Console и `.env`
3. Убедитесь, что backend перезапущен после изменения `.env`

### Диплинк не работает

1. Убедитесь, что scheme `dropscrypto` одинаковый везде:
   - `drops-crypto-app/app.json` → `"scheme": "dropscrypto"`
   - `drops-crypto-api/.env` → `APP_DEEPLINK_SCHEME=dropscrypto`
2. Переустановите приложение в Expo Go

## 🎯 После успешной настройки

Когда все работает, можно переходить к:

1. Добавлению навигации (Profile/Streamers/Prizes)
2. Реализации запроса `/me` с авторизацией
3. Созданию экрана добавления кошелька
4. Улучшению UI

---

**Все готово для запуска!** Следуйте шагам выше по порядку. 🚀
