# 🚀 НАЧНИТЕ ЗДЕСЬ - Быстрый старт

## ✅ Что уже готово

- ✅ Docker контейнеры запущены (PostgreSQL + Redis)
- ✅ База данных настроена
- ✅ Backend работает на http://localhost:3000
- ✅ Все зависимости установлены

## 🎯 Быстрый запуск (3 минуты)

### Шаг 1: Установите ngrok (если еще не установлен)

**Вариант A - Автоматически:**
```powershell
powershell -ExecutionPolicy Bypass -File .\setup-ngrok.ps1
```

**Вариант B - Вручную:**
1. Скачайте с https://ngrok.com/download
2. Распакуйте `ngrok.exe` в папку
3. Добавьте папку в PATH

### Шаг 2: Получите Twitch ключи

1. Откройте https://dev.twitch.tv/console/apps
2. Создайте приложение (или используйте существующее)
3. Скопируйте **Client ID** и создайте **Client Secret**

### Шаг 3: Настройте .env

Откройте `drops-crypto-api\.env` и замените:
```
TWITCH_CLIENT_ID=your_twitch_client_id_here
TWITCH_CLIENT_SECRET=your_twitch_client_secret_here
```
на ваши реальные ключи.

### Шаг 4: Запустите ngrok

В новом терминале PowerShell:
```powershell
ngrok http 3000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

### Шаг 5: Обновите URL везде

```powershell
.\update-ngrok-url.ps1 -NgrokUrl "https://ваш-url.ngrok-free.app"
```

Или обновите вручную:
- `drops-crypto-api\.env` → `PUBLIC_BASE_URL` и `TWITCH_REDIRECT_URI`
- `drops-crypto-app\App.tsx` → `API_BASE`

### Шаг 6: Настройте Twitch Console

1. https://dev.twitch.tv/console/apps
2. Ваше приложение → OAuth Redirect URLs
3. Добавьте: `https://ваш-url.ngrok-free.app/auth/twitch/callback`

### Шаг 7: Перезапустите Backend

```powershell
cd drops-crypto-api
npm run start:dev
```

### Шаг 8: Запустите мобильное приложение

```powershell
cd drops-crypto-app
npm start
```

Откройте Expo Go на телефоне и отсканируйте QR код.

## ✅ Проверка

1. http://localhost:3000/health → `{"ok":true}`
2. https://ваш-url.ngrok-free.app/health → `{"ok":true}` (с телефона)
3. В приложении нажмите "Привязать Twitch" → должен открыться login
4. После авторизации должно вернуться в приложение с токеном

## 📚 Дополнительная документация

- `NEXT_ACTIONS.md` - Подробные инструкции
- `AUTOMATION.md` - Все доступные скрипты
- `SETUP.md` - Детальная настройка

---

**Всё готово к запуску!** 🎉
