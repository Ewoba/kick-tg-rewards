# Чеклист настройки Drops Crypto

Используйте этот чеклист для пошаговой настройки проекта.

## ✅ Backend Setup

- [ ] Установлены зависимости (`cd drops-crypto-api && npm install`)
- [ ] Docker Compose запущен (`docker compose up -d`)
- [ ] PostgreSQL работает (проверьте: `docker ps`)
- [ ] Создан `.env` файл (скопирован из `.env.example`)
- [ ] Заполнены все переменные в `.env`:
  - [ ] `DATABASE_URL`
  - [ ] `TWITCH_CLIENT_ID`
  - [ ] `TWITCH_CLIENT_SECRET`
  - [ ] `JWT_SECRET`
  - [ ] `PUBLIC_BASE_URL` (пока localhost)
  - [ ] `TWITCH_REDIRECT_URI` (пока localhost)
- [ ] Выполнена миграция Prisma (`npx prisma migrate dev --name init`)
- [ ] Сгенерирован Prisma Client (`npx prisma generate`)
- [ ] Backend запускается без ошибок (`npm run start:dev`)
- [ ] Health endpoint работает: `http://localhost:3000/health` → `{"ok":true}`

## ✅ ngrok Setup

- [ ] ngrok установлен
- [ ] Backend запущен на порту 3000
- [ ] ngrok запущен в отдельном терминале (`ngrok http 3000`)
- [ ] Получен публичный HTTPS URL от ngrok (например: `https://xxxxx.ngrok-free.app`)
- [ ] Health endpoint доступен с телефона через ngrok URL
- [ ] Обновлен `.env` в backend:
  - [ ] `PUBLIC_BASE_URL` = ngrok URL
  - [ ] `TWITCH_REDIRECT_URI` = ngrok URL + `/auth/twitch/callback`
- [ ] Backend перезапущен после изменения `.env`

## ✅ Twitch Developer Console

- [ ] Перешли на https://dev.twitch.tv/console/apps
- [ ] Создали приложение (или используете существующее)
- [ ] Скопировали Client ID в `.env`
- [ ] Создали Client Secret и скопировали в `.env`
- [ ] Добавили Redirect URL: `https://xxxxx.ngrok-free.app/auth/twitch/callback`
- [ ] Сохранили изменения в Twitch Console

## ✅ Mobile App Setup

- [ ] Установлены зависимости (`cd drops-crypto-app && npm install`)
- [ ] Обновлен `API_BASE` в `App.tsx` на ngrok URL
- [ ] Установлен Expo Go на телефон (или настроен эмулятор)
- [ ] Запущено приложение (`npm start`)
- [ ] Приложение открывается в Expo Go

## ✅ Тестирование OAuth

- [ ] В приложении видна кнопка "Привязать Twitch"
- [ ] При нажатии открывается Twitch login
- [ ] Успешно авторизуется через Twitch
- [ ] После авторизации возвращается в приложение
- [ ] В приложении показывается "Token: есть"

## ✅ Контрольная точка пройдена

Если все пункты выше выполнены:
- [ ] Backend доступен через ngrok
- [ ] Health endpoint открывается с телефона
- [ ] Twitch OAuth работает
- [ ] Приложение получает токен

## 🎯 Следующие шаги

После успешной контрольной точки:
- [ ] Добавить навигацию (Profile/Streamers/Prizes)
- [ ] Реализовать запрос `/me` с авторизацией
- [ ] Создать экран добавления кошелька
- [ ] Улучшить UI

## 🐛 Troubleshooting

Если что-то не работает, проверьте:

### Backend не запускается
- Docker запущен? (`docker ps`)
- PostgreSQL работает? (`docker compose logs postgres`)
- `.env` файл существует и заполнен?
- Порт 3000 свободен?

### ngrok не работает
- Backend запущен на 3000?
- ngrok запущен в отдельном терминале?
- URL изменился? (обновите `.env`)

### Twitch OAuth не работает
- Redirect URL правильный в Twitch Console?
- Client ID и Secret правильные?
- ngrok URL обновлен в `.env`?
- Backend перезапущен после изменения `.env`?

### Диплинк не работает
- Scheme `dropscrypto` везде совпадает?
- Приложение переустановлено в Expo Go?
- Проверьте логи в `App.tsx`
