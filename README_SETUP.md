# 🚀 Быстрый запуск Drops Crypto

## ✅ Что уже готово

- ✅ Зависимости установлены в `drops-crypto-api`
- ✅ Зависимости установлены в `drops-crypto-app`
- ✅ `.env` файл создан в `drops-crypto-api`
- ✅ Prisma Client сгенерирован
- ✅ Backend компилируется без ошибок
- ✅ PowerShell скрипты для автоматизации созданы

## ⚡ Самый быстрый способ запуска

### 1. Запустите Docker Desktop

Откройте Docker Desktop из меню Пуск (Windows) или Applications (Mac).

### 2. Запустите всё автоматически

```powershell
.\start-all.ps1
```

Если у вас возникают проблемы с вводом команд в терминале (например, странные символы перед командами), используйте Windows batch файл и запустите его двойным кликом:

```
start-all.bat
```

Этот скрипт:
- ✅ Проверит Docker
- ✅ Запустит Docker Compose
- ✅ Запустит backend в отдельном окне
- ✅ Запустит ngrok в отдельном окне
- ✅ Запустит мобильное приложение в отдельном окне

### 3. Настройте Twitch (1 минута)

1. Скопируйте ngrok HTTPS URL из окна ngrok (например: `https://abc123.ngrok-free.app`)

2. Обновите URL везде одной командой:
   ```powershell
   .\update-ngrok-url.ps1 -NgrokUrl "https://ваш-ngrok-url.ngrok-free.app"
   ```

3. Настройте Twitch Developer Console:
   - Перейдите на https://dev.twitch.tv/console/apps
   - Откройте ваше приложение
   - В "OAuth Redirect URLs" добавьте: `https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback`
   - Сохраните изменения

4. Откройте `drops-crypto-api/.env` и замените:
   ```
   TWITCH_CLIENT_ID=your_twitch_client_id_here
   TWITCH_CLIENT_SECRET=your_twitch_client_secret_here
   ```
   на ваши реальные ключи из Twitch Developer Console

5. Перезапустите backend (остановите окно backend и запустите снова):
   ```powershell
   cd drops-crypto-api
   .\start-backend.ps1
   ```

### 4. Быстрая проверка

После запуска всех окон выполните быструю проверку:
```powershell
.\verify-setup.ps1
```
Скрипт проверит `/health`, `/auth/twitch/start` и доступность ngrok туннеля.

### 4. Готово! 🎉

Откройте Expo Go на телефоне и отсканируйте QR код из окна мобильного приложения.

## 📋 Подробные инструкции

- **AUTOMATION.md** - Все доступные скрипты и их использование
- **SETUP.md** - Детальная пошаговая инструкция
- **CHECKLIST.md** - Чеклист для проверки всех шагов
- **QUICK_START.md** - Краткая инструкция

## 🔧 Альтернативный способ (если скрипты не работают)

### Шаг 1: Настройка Backend

```powershell
cd drops-crypto-api
docker compose up -d
npx prisma migrate dev --name init
```

### Шаг 2: Настройка Twitch

1. Обновите `TWITCH_CLIENT_ID` и `TWITCH_CLIENT_SECRET` в `.env`

### Шаг 3: Запуск Backend

```powershell
npm run start:dev
```

### Шаг 4: Запуск ngrok (в новом терминале)

```powershell
cd ..
ngrok http 3000
```

### Шаг 5: Обновление URL

1. Скопируйте ngrok URL
2. Обновите в `.env`: `PUBLIC_BASE_URL` и `TWITCH_REDIRECT_URI`
3. Обновите в `drops-crypto-app/App.tsx`: `API_BASE`
4. Добавьте в Twitch Developer Console: Redirect URL

### Шаг 6: Запуск Mobile App

```powershell
cd drops-crypto-app
npm start
```

## ⚠️ Важно

1. **Docker Desktop должен быть запущен** перед выполнением любых команд с Docker
2. **Twitch ключи** нужно получить и добавить в `.env` вручную
3. **ngrok URL** меняется при перезапуске ngrok - обновите его везде
4. **Backend нужно перезапустить** после изменения `.env`

## ✅ Контрольная точка

Все работает, если:
- ✅ `http://localhost:3000/health` возвращает `{"ok":true}`
- ✅ `https://ваш-ngrok-url.ngrok-free.app/health` открывается с телефона
- ✅ При нажатии "Привязать Twitch" открывается Twitch login
- ✅ После авторизации возвращается в приложение
- ✅ В приложении показывается "Token: есть"

## 🆘 Проблемы?

См. `AUTOMATION.md` раздел "Решение проблем"
