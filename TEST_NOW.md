# 🚀 Тестирование СЕЙЧАС (эмулятор уже запущен!)

## ✅ Ваш эмулятор работает!

## Быстрые шаги:

### 1. Установите Expo Go на эмуляторе

1. На эмуляторе откройте **Google Play Store**
2. Найдите **"Expo Go"**
3. Установите приложение
4. Откройте Expo Go

### 2. Настройте API_BASE для эмулятора

Откройте файлы и временно замените `API_BASE`:

**В `drops-crypto-app/App.tsx` и всех `screens/*.tsx`:**

```typescript
// Для Android эмулятора используйте:
const API_BASE = 'http://10.0.2.2:3000';

// ИЛИ если используете ngrok:
const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
```

**Важно:** `10.0.2.2` - это специальный IP, который Android эмулятор использует для доступа к localhost вашего компьютера.

### 3. Убедитесь, что Backend работает

```powershell
# Проверка
Invoke-RestMethod -Uri http://localhost:3000/health
```

Если не работает, запустите:
```powershell
cd drops-crypto-api
npm run start:dev
```

### 4. Запустите Mobile App

```powershell
cd drops-crypto-app
npm start
```

В терминале Expo:
- Нажмите **`a`** для Android эмулятора
- ИЛИ откройте Expo Go на эмуляторе и отсканируйте QR код

### 5. Протестируйте

1. **Авторизация:**
   - Нажмите "Привязать Twitch"
   - Откроется браузер на эмуляторе
   - Войдите в Twitch
   - Должно вернуться в приложение

2. **Экраны:**
   - Переключайтесь между вкладками
   - Проверьте список стримеров
   - Проверьте список призов
   - Откройте профиль

3. **Функции:**
   - Добавьте кошелёк (адрес формата 0x...)
   - Попробуйте получить приз

## Если что-то не работает:

### Backend недоступен с эмулятора

**Проблема:** `http://10.0.2.2:3000` не работает

**Решение:**
1. Используйте ngrok (рекомендуется)
2. Или используйте реальный IP вашей сети:
   ```powershell
   ipconfig | Select-String "IPv4"
   # Используйте этот IP вместо 10.0.2.2
   ```

### Deep linking не работает

**Проблема:** После OAuth не возвращается в приложение

**Решение:**
- Deep linking на эмуляторе может работать не так, как на реальном устройстве
- Для полного тестирования OAuth используйте реальное устройство
- Или тестируйте через браузер эмулятора вручную

### Expo Go не запускается

**Решение:**
```powershell
# Проверить подключение
adb devices

# Перезапустить Expo
cd drops-crypto-app
npm start
```

## Альтернатива: Web версия

Для быстрого тестирования UI без эмулятора:

```powershell
cd drops-crypto-app
npm start
# Нажмите 'w' для web
```

Откроется в браузере. Ограничения: некоторые функции могут не работать.

---

**Всё готово! Приступайте к тестированию!** 🎉
