# 🤖 Автоматизация запуска проекта

Созданы PowerShell скрипты для автоматизации запуска проекта.

## 📋 Доступные скрипты

### 1. `start-all.ps1` - Запустить всё сразу

**Запуск:**
```powershell
.\start-all.ps1
```

**Что делает:**
- ✅ Проверяет Docker
- ✅ Запускает Docker Compose (если не запущен)
- ✅ Запускает backend в отдельном окне
- ✅ Запускает ngrok в отдельном окне (если установлен)
- ✅ Запускает мобильное приложение в отдельном окне

### 2. `drops-crypto-api/setup.ps1` - Настройка Backend

**Запуск:**
```powershell
cd drops-crypto-api
.\setup.ps1
```

**Что делает:**
- ✅ Проверяет Docker
- ✅ Запускает Docker Compose
- ✅ Генерирует Prisma Client
- ✅ Выполняет миграции
- ✅ Проверяет .env файл

### 3. `drops-crypto-api/start-backend.ps1` - Запуск Backend

**Запуск:**
```powershell
cd drops-crypto-api
.\start-backend.ps1
```

**Что делает:**
- ✅ Проверяет .env файл
- ✅ Запускает backend на http://localhost:3000

### 4. `start-ngrok.ps1` - Запуск ngrok

**Запуск:**
```powershell
.\start-ngrok.ps1
```

**Что делает:**
- ✅ Проверяет наличие ngrok
- ✅ Запускает ngrok на порт 3000

### 5. `update-ngrok-url.ps1` - Обновить ngrok URL

**Запуск:**
```powershell
.\update-ngrok-url.ps1 -NgrokUrl "https://xxxxx.ngrok-free.app"
```

**Что делает:**
- ✅ Обновляет `PUBLIC_BASE_URL` в `drops-crypto-api/.env`
- ✅ Обновляет `TWITCH_REDIRECT_URI` в `drops-crypto-api/.env`
- ✅ Обновляет `API_BASE` в `drops-crypto-app/App.tsx`

**Пример:**
```powershell
.\update-ngrok-url.ps1 -NgrokUrl "https://abc123.ngrok-free.app"
```

## 🚀 Быстрый старт

### Вариант 1: Автоматический (рекомендуется)

1. **Убедитесь, что Docker Desktop запущен**

2. **Запустите всё сразу:**
   ```powershell
   .\start-all.ps1
   ```

3. **Скопируйте ngrok URL из окна ngrok**

4. **Обновите URL везде:**
   ```powershell
   .\update-ngrok-url.ps1 -NgrokUrl "https://ваш-ngrok-url.ngrok-free.app"
   ```

5. **Настройте Twitch Developer Console:**
   - Перейдите на https://dev.twitch.tv/console/apps
   - Добавьте Redirect URL: `https://ваш-ngrok-url.ngrok-free.app/auth/twitch/callback`

6. **Перезапустите backend** (остановите и запустите снова)

### Вариант 2: Пошаговый

1. **Настройте Backend:**
   ```powershell
   cd drops-crypto-api
   .\setup.ps1
   ```

2. **Обновите Twitch ключи в `.env`:**
   - Откройте `drops-crypto-api/.env`
   - Замените `your_twitch_client_id_here` и `your_twitch_client_secret_here`

3. **Запустите Backend:**
   ```powershell
   .\start-backend.ps1
   ```

4. **В новом терминале запустите ngrok:**
   ```powershell
   cd ..
   .\start-ngrok.ps1
   ```

5. **Скопируйте ngrok URL и обновите:**
   ```powershell
   .\update-ngrok-url.ps1 -NgrokUrl "https://ваш-ngrok-url.ngrok-free.app"
   ```

6. **Настройте Twitch Developer Console** (см. выше)

7. **Запустите Mobile App:**
   ```powershell
   cd drops-crypto-app
   npm start
   ```

## ⚠️ Требования

Перед использованием скриптов убедитесь:

- ✅ Docker Desktop **запущен**
- ✅ Node.js 20+ установлен
- ✅ npm установлен
- ✅ ngrok установлен (опционально, но рекомендуется)

## 🔧 Ручные шаги

Скрипты не могут выполнить:

1. **Запустить Docker Desktop** - запустите вручную из меню Пуск
2. **Настроить Twitch ключи** - добавьте в `.env` файл
3. **Настроить Twitch Developer Console** - через веб-интерфейс

## 📝 Полезные команды

### Проверить статус Docker
```powershell
docker ps
```

### Проверить логи контейнеров
```powershell
docker compose logs
```

### Остановить контейнеры
```powershell
cd drops-crypto-api
docker compose down
```

### Перезапустить контейнеры
```powershell
cd drops-crypto-api
docker compose restart
```

### Просмотреть миграции
```powershell
cd drops-crypto-api
npx prisma migrate status
```

### Открыть Prisma Studio
```powershell
cd drops-crypto-api
npx prisma studio
```

## 🐛 Решение проблем

### Docker не запускается
- Убедитесь, что Docker Desktop установлен и запущен
- Проверьте в трее Windows, что Docker Desktop работает

### Ошибка подключения к БД
- Убедитесь, что Docker Compose запущен: `docker ps`
- Проверьте логи: `docker compose logs postgres`

### ngrok не найден
- Установите ngrok: https://ngrok.com/download
- Добавьте ngrok в PATH или используйте полный путь

### Backend не компилируется
- Проверьте, что все зависимости установлены: `npm install`
- Проверьте TypeScript ошибки в выводе
