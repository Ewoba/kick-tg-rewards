# ⚡ Быстрый запуск на эмуляторе

## Самый простой способ

### 1. Проверка эмулятора

```powershell
# Проверить, установлен ли Android SDK
adb devices
```

Если команда не найдена, установите Android Studio (см. `EMULATOR_GUIDE.md`)

### 2. Запуск эмулятора

**Способ A: Через скрипт**
```powershell
powershell -ExecutionPolicy Bypass -File .\start-emulator.ps1
```

**Способ B: Через Android Studio**
1. Откройте Android Studio
2. Tools → Device Manager
3. Нажмите ▶ рядом с нужным эмулятором

**Способ C: Вручную**
```powershell
$emulatorPath = "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe"
& $emulatorPath -list-avds  # Список эмуляторов
& $emulatorPath -avd имя_эмулятора  # Запустить
```

### 3. Установка Expo Go на эмулятор

1. Дождитесь полной загрузки эмулятора
2. Откройте Google Play Store
3. Найдите "Expo Go"
4. Установите

### 4. Настройка для локального доступа

Для тестирования на эмуляторе можно использовать локальный адрес:

**Временно обновите `API_BASE` в `App.tsx`:**
```typescript
// Для Android эмулятора (10.0.2.2 указывает на localhost хост-машины)
const API_BASE = 'http://10.0.2.2:3000';

// Или используйте ngrok URL (рекомендуется)
const API_BASE = 'https://ваш-ngrok-url.ngrok-free.app';
```

**Важно:** `10.0.2.2` - это специальный IP, который Android эмулятор использует для доступа к localhost хост-машины.

### 5. Запуск приложения

```powershell
# Терминал 1: Backend
cd drops-crypto-api
npm run start:dev

# Терминал 2: ngrok (опционально, если используете ngrok)
ngrok http 3000

# Терминал 3: Mobile App
cd drops-crypto-app
npm start
```

В Expo CLI:
- Нажмите `a` для запуска на Android эмуляторе
- Или откройте Expo Go на эмуляторе и отсканируйте QR код

### 6. Тестирование

1. ✅ Проверьте, что приложение открылось на эмуляторе
2. ✅ Протестируйте авторизацию (может открыться браузер эмулятора)
3. ✅ Проверьте все экраны
4. ✅ Протестируйте добавление кошелька
5. ✅ Протестируйте получение приза

## Важные моменты

- **Deep linking:** На эмуляторе может работать не так, как на реальном устройстве
- **Производительность:** Первый запуск может быть медленным
- **Интернет:** Эмулятор должен иметь доступ в интернет для OAuth

## Альтернатива: Web версия (самый быстрый)

Если нужно быстро протестировать UI без эмулятора:

```powershell
cd drops-crypto-app
npm start
# Нажмите 'w' для web версии
```

Откроется в браузере. Ограничения: deep linking может не работать.

---

**Готово к тестированию!** 🚀
