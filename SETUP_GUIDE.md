# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ: Запуск и тестирование на Android Studio Emulator

## ⚠️ ВАЖНЫЕ НЮАНСЫ ДЛЯ WINDOWS + ANDROID STUDIO + EXPO

Это пошаговая инструкция с учетом всех нюансов для Windows, Docker Desktop, Android Studio Emulator и Expo/React Native.

---

## 📋 Часть 0: Что уже реализовано и что нужно доделать

### ✅ Уже реализовано:
- Docker: Postgres + Redis
- Prisma schema + миграции + seed
- Все API endpoints (20+ endpoints)
- Мобильное приложение: экраны и навигация

### ❌ Что НЕ реализовано (это нормально):
- Реальная крипто-выплата on-chain (PENDING → SUCCESS/FAILED автоматически)
- SIWE/подпись кошелька (заготовки есть, но не включены)
- EventSub automation (отложено)

### ✅ Что нужно доделать сейчас:
- Установить зависимости мобильного приложения
- Запустить backend
- Запустить мобилку в эмуляторе
- Протестировать основной функционал

---

## 🔧 Часть 1: Подготовка окружения (ОБЯЗАТЕЛЬНО)

### 1.1 Docker Desktop

1. **Запустите Docker Desktop**
2. Дождитесь статуса "Docker is running"
3. Проверьте:
   ```powershell
   docker version
   ```

### 1.2 Android Studio + Emulator

1. Откройте **Android Studio**
2. Перейдите: **Tools → Device Manager**
3. Создайте устройство (например, Pixel 6):
   - **Образ**: Google Play или Google APIs
   - **API Level**: 33 или выше
4. **Запустите эмулятор** (кнопка Play ▶️)
5. Дождитесь полной загрузки (должен открыться экран Android)

**⚠️ ВАЖНО**: Эмулятор должен быть запущен ДО запуска Expo!

---

## 🗄️ Часть 2: Backend (Postgres/Redis + Prisma + Seed + API)

### 2.1 Поднять БД и Redis

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-api
docker compose up -d
```

Проверьте, что контейнеры запущены:
```powershell
docker ps
```

**Ожидаете увидеть 2 контейнера:**
- `drops-crypto-api-postgres-1`
- `drops-crypto-api-redis-1`

### 2.2 Проверить .env файл

Откройте: `C:\Users\Admin\Downloads\drops\drops-crypto-api\.env`

**Минимально должно быть:**
```env
DATABASE_URL=postgresql://drops:drops@localhost:5432/drops
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret

# Twitch можно не заполнять, если не тестируете OAuth
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
PUBLIC_BASE_URL=
TWITCH_REDIRECT_URI=
APP_DEEPLINK_SCHEME=dropscrypto
```

**⚠️ ВАЖНО**: Логин/пароль/база должны совпадать с `docker-compose.yml`.

### 2.3 Prisma: миграции + seed

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-api

# Применить миграции
npx prisma migrate dev

# Заполнить тестовыми данными
npx prisma db seed
```

**Если `npx prisma db seed` не работает**, используйте:
```powershell
npx ts-node prisma/seed.ts
```

### 2.4 Запустить backend

```powershell
npm run start:dev
```

Дождитесь сообщения: `Nest application successfully started`

### 2.5 ✅ Контрольная точка: Проверка API

Откройте в браузере на ПК:

1. **Health check:**
   ```
   http://localhost:3000/health
   ```
   Должно вернуть: `{ "ok": true }`

2. **Стримеры:**
   ```
   http://localhost:3000/streamers
   ```
   Должен вернуть JSON-массив стримеров

3. **Призы:**
   ```
   http://localhost:3000/prizes
   ```
   Должен вернуть JSON-массив призов

**✅ Если все три работают — backend полностью живой!**

---

## 📱 Часть 3: Мобильное приложение (установка зависимостей + запуск)

### 3.1 Установить зависимости

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-app

# Установить зависимости навигации и axios
npm install

# Или если нужно установить вручную:
npm install axios @react-navigation/native @react-navigation/native-stack @react-navigation/bottom-tabs
npx expo install react-native-screens react-native-safe-area-context react-native-gesture-handler
```

### 3.2 ✅ КРИТИЧЕСКИЙ НЮАНС: react-native-gesture-handler

**ВАЖНО**: В файле `App.tsx` первой строкой должен быть:
```typescript
import 'react-native-gesture-handler';
```

**Это уже исправлено в коде**, но проверьте файл `drops-crypto-app/App.tsx` — первая строка должна быть именно такой.

### 3.3 ✅ НЮАНС СЕТИ: URL для эмулятора

**В Android Emulator `localhost` = сам эмулятор, НЕ ваш ПК!**

Чтобы обратиться к backend на вашем ПК, используйте: **`http://10.0.2.2:3000`**

**✅ Это уже настроено во всех экранах**, но проверьте:
- `App.tsx` (если используется)
- `screens/AuthScreen.tsx`
- `screens/ProfileScreen.tsx`
- `screens/StreamersScreen.tsx`
- `screens/PrizesScreen.tsx`
- `screens/MyPrizesScreen.tsx`

Везде должно быть:
```typescript
const API_BASE = 'http://10.0.2.2:3000';
```

### 3.4 Запуск Expo на эмуляторе

**⚠️ Убедитесь, что эмулятор уже запущен!**

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-app
npm start
```

В окне Metro Bundler:
1. Дождитесь появления QR-кода и меню
2. Нажмите клавишу **`a`** (запуск на Android)
3. Или выберите опцию **"Press a │ open Android"**

### 3.5 ✅ Контрольная точка: Мобильное приложение

**Приложение должно:**
- ✅ Открыться в эмуляторе
- ✅ Не падать при переключении вкладок
- ✅ Показывать данные (стримеры/призы) если backend запущен

**Если приложение падает:**
1. Проверьте, что backend запущен (`http://localhost:3000/health`)
2. Проверьте, что в коде `API_BASE = 'http://10.0.2.2:3000'`
3. Проверьте логи в Metro Bundler (красные ошибки)

---

## 🧪 Часть 4: Тестирование "как LinkDrops" (без Twitch OAuth)

### 4.1 Сценарий теста (без авторизации)

**Что можно протестировать без OAuth:**
1. ✅ **Стримеры**: Откройте вкладку "Стримеры" → должны быть seeded стримеры
2. ✅ **Призы**: Откройте вкладку "Призы" → должны быть seeded призы

**Что требует авторизации (будет ошибка 401):**
- ❌ Добавление кошелька
- ❌ Claim приза
- ❌ Подписка на стримеров
- ❌ Мои призы

**Это нормально!** Для полного теста нужен OAuth (см. Часть 5).

### 4.2 Быстрые проверки через PowerShell

**Проверить стримеров:**
```powershell
Invoke-RestMethod -Uri http://localhost:3000/streamers | ConvertTo-Json
```

**Проверить призы:**
```powershell
Invoke-RestMethod -Uri http://localhost:3000/prizes | ConvertTo-Json
```

---

## 🔐 Часть 5: Twitch OAuth в эмуляторе (ОПЦИОНАЛЬНО)

**⚠️ ВАЖНО**: OAuth работает только с ngrok и требует настройки Dev Client для deep linking.

### 5.1 Почему "просто Expo Go" может не работать

Кастомная схема `dropscrypto://` нормально работает в:
- ✅ **Dev Client** (expo-dev-client) — рекомендовано
- ✅ **Production build**
- ⚠️ **Expo Go** — может не открывать кастомные схемы предсказуемо

**Рекомендация**: Включайте OAuth только после того, как UI + API уже работают (после Части 4).

### 5.2 Подготовка ngrok

**Установить ngrok (если нет):**
```powershell
winget install ngrok.ngrok
```

**Или скачать**: https://ngrok.com/download

**Запустить ngrok:**
```powershell
ngrok http 3000
```

**Скопируйте URL**: `https://xxxxx.ngrok-free.app`

### 5.3 Обновить backend .env для OAuth

Откройте `drops-crypto-api\.env` и обновите:
```env
PUBLIC_BASE_URL=https://xxxxx.ngrok-free.app
TWITCH_REDIRECT_URI=https://xxxxx.ngrok-free.app/auth/twitch/callback
APP_DEEPLINK_SCHEME=dropscrypto
TWITCH_CLIENT_ID=ВАШ_ID
TWITCH_CLIENT_SECRET=ВАШ_SECRET
```

**Перезапустите backend:**
```powershell
# Остановите (Ctrl+C) и запустите снова
npm run start:dev
```

### 5.4 Twitch Developer Console

1. Перейдите: https://dev.twitch.tv/console/apps
2. Откройте ваше приложение (или создайте новое)
3. В **"OAuth Redirect URLs"** добавьте:
   ```
   https://xxxxx.ngrok-free.app/auth/twitch/callback
   ```

### 5.5 Включить Dev Client (чтобы deep link точно отработал)

**⚠️ Требуется для надежного deep linking:**

```powershell
cd C:\Users\Admin\Downloads\drops\drops-crypto-app

# Установить expo-dev-client
npx expo install expo-dev-client

# Создать dev build (займет несколько минут)
npx expo run:android
```

Это установит dev build на эмулятор. После этого deep link `dropscrypto://auth?...` будет нормально открываться.

### 5.6 Тест OAuth

1. В приложении нажмите **"Привязать Twitch"**
2. Откроется браузер → логин в Twitch
3. После авторизации → callback → редирект `dropscrypto://auth?token=...`
4. Приложение ловит token и сохраняет
5. Теперь доступны все функции (кошелёк, claim, подписки)

---

## ✅ Часть 6: Контрольные точки (проверка что всё работает)

### Проверьте по порядку:

1. **✅ Docker работает:**
   ```powershell
   docker ps
   ```
   Должно быть 2 контейнера (postgres, redis)

2. **✅ Backend отвечает:**
   ```
   http://localhost:3000/health → { "ok": true }
   ```

3. **✅ API возвращает данные:**
   ```
   http://localhost:3000/streamers → JSON массив
   http://localhost:3000/prizes → JSON массив
   ```

4. **✅ Мобилка открылась:**
   - Приложение запустилось в эмуляторе
   - Нет красных ошибок в Metro

5. **✅ Мобилка видит backend:**
   - Вкладка "Стримеры" показывает данные
   - Вкладка "Призы" показывает данные

**Если пункт 5 не работает:**
- Проверьте, что backend запущен
- Проверьте `API_BASE = 'http://10.0.2.2:3000'` во всех экранах
- Проверьте логи в Metro Bundler

---

## 🐛 Часть 7: Типичные проблемы и решения

### Проблема: "Cannot find module '@react-navigation/native'"

**Решение:**
```powershell
cd drops-crypto-app
npm install
```

### Проблема: Backend не запускается "Cannot connect to database"

**Решение:**
1. Проверьте, что Docker запущен: `docker ps`
2. Проверьте `.env` файл (DATABASE_URL)
3. Перезапустите контейнеры: `docker compose restart`

### Проблема: Мобилка не видит backend (нет данных)

**Решение:**
1. Проверьте `API_BASE = 'http://10.0.2.2:3000'` (не `localhost`!)
2. Проверьте, что backend запущен
3. Проверьте логи в Metro Bundler (красные ошибки)

### Проблема: "Cannot find module 'react-native-gesture-handler'"

**Решение:**
```powershell
cd drops-crypto-app
npx expo install react-native-gesture-handler
```

И проверьте, что в `App.tsx` первой строкой:
```typescript
import 'react-native-gesture-handler';
```

### Проблема: Deep link не работает (OAuth callback)

**Решение:**
1. Используйте Dev Client: `npx expo run:android`
2. Проверьте `APP_DEEPLINK_SCHEME=dropscrypto` в `.env`
3. Проверьте `app.json`: `"scheme": "dropscrypto"`

---

## 📝 Часть 8: Что дальше (опционально)

### 8.1 Админское подтверждение claims (самый простой MVP)

Для тестирования статусов без реального блокчейна:

**Добавить endpoints:**
- `POST /admin/claims/:id/mark-success`
- `POST /admin/claims/:id/mark-failed`

Это позволит "поиграться" статусами и увидеть в UI фильтры (В процессе / Успешно / Ошибка).

### 8.2 Реальный тестнет-процессинг (Base Sepolia)

Минимально нужно:
- Конфиг RPC URL
- Hot-wallet private key в `.env`
- Mock token ERC20 "USDC" на Base Sepolia
- Очередь (BullMQ) которая:
  - Берёт PENDING claim
  - Делает transfer
  - Пишет txHash
  - Ждёт confirm и ставит SUCCESS/FAILED

**Это подключаем после того, как core UX работает.**

---

## 🎯 ИТОГ: Что работает после выполнения инструкции

### ✅ Рабочие (без OAuth):
- Каталог стримеров
- Каталог призов
- Навигация в приложении

### ✅ Рабочие (с OAuth):
- Логин через Twitch
- Привязка кошелька (в БД)
- Claim приза (создание PENDING)
- "Мои призы" (claims) с фильтрами по статусу
- Подписка на стримеров

### ❌ Пока не рабочее (это нормально на Phase 1-2):
- Реальная крипто-выплата (tx отправка/подтверждение)
- SIWE/верификация кошелька (CryptoModule не подключён)
- Автоматическое изменение статусов (PENDING → SUCCESS/FAILED)

---

## 📞 Следующий шаг

**Сделайте контрольные точки и напишите результат:**

1. ✅ `http://localhost:3000/health` работает?
2. ✅ `http://localhost:3000/streamers` и `/prizes` работают?
3. ✅ Мобилка открылась на эмуляторе?
4. ✅ Мобилка видит backend (данные грузятся)?

**Если пункт 4 не работает — в 90% случаев причина в URL (нужно `10.0.2.2`).**

---

**Удачи! 🚀**
